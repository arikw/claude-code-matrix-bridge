{
  "name": "rx-claude-matrix-bridge",
  "version": "0.4.0",
  "description": "Matrix as a Claude Code Channel: live two-way bridge with always-on daemon + per-session routing + headless fallback for offline TUIs.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx server.ts",
    "daemon": "tsx daemon.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "author": "arikw"
}
